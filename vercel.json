{
  "buildCommand": "npm --prefix \"student portal\" install && npm --prefix \"student portal\" run build",
  "outputDirectory": "student portal/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
